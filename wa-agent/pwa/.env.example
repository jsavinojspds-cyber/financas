# Copie para .env. Estas duas variaveis vao para o bundle do navegador —
# e por isso que aqui e a ANON key, nunca a service_role.
#
# A anon key e publica por design. A protecao vem do 006_pwa.sql: ela nao
# le tabela nenhuma, e a funcao do painel exige login e whitelist de e-mail.

VITE_SUPABASE_URL=https://ycakggiaklceubevkhag.supabase.co

# Painel Supabase > Settings > API > anon / public
VITE_SUPABASE_ANON_KEY=
